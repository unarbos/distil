# SN97 King uses an open OpenAI-compatible endpoint at https://api.arbos.life/v1.
# The endpoint does NOT require auth, but pi-ai's OpenAI client constructor
# refuses to start without an API key, so we set a sentinel value. It is never
# transmitted to the king.
OPENAI_API_KEY=sn97-public-no-auth-required
